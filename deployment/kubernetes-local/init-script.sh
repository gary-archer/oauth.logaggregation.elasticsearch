#!/bin/bash

####################################################################
# A script used by the init job to wait until Elasticsearch is ready
####################################################################

cd "$(dirname "${BASH_SOURCE[0]}")"
